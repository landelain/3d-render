{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++",
            "args": [
                "-g",
                "${workspaceFolder}/project1.cpp",  
                "-o",
                "${workspaceFolder}/project1"  
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
